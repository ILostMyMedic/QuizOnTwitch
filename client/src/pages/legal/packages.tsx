import Container from '../../layout/container';
import Headline from '../../components/headline';

const PackagesPage = () => {
    return (
        <Container>
            <Headline headlineText="Packages and Licenses" subHeadlineText="Updated 11.05.2024" />
        </Container>
    );
};

export default PackagesPage;
